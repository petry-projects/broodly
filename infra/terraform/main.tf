# Broodly Infrastructure — Terraform Root
# This file serves as the entry point for Terraform configuration.
# Environment-specific configurations are in environments/<env>/.

terraform {
  required_version = ">= 1.5"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 5.0"
    }
  }
}
